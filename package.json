{
  "scripts": {
    "build": "babel src --out-file bundle.js",
    "launch" : "node bundle",
    "start": "yarn run build && yarn run launch"
  },
  "name": "node_starters1",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
